name: Security Scan

on:
  schedule:
    # Run security scan weekly on Sundays at 2 AM UTC
    - cron: '0 2 * * 0'
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'lib/**'
      - 'android/**'
      - 'pubspec.yaml'
      - '.github/workflows/security.yml'

jobs:
  flutter-security:
    name: Flutter Security Check
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        cache: true
        
    - name: Get Flutter dependencies
      run: flutter pub get
      
    - name: Check for hardcoded secrets in Dart code
      run: |
        echo "🔍 Scanning Dart code for potential secrets..."
        
        # Check for common secret patterns
        if grep -r -i "password\s*=\s*['\"][^'\"]*['\"]" lib/ --include="*.dart"; then
          echo "⚠️ Found potential hardcoded passwords"
        fi
        
        if grep -r -i "api[_-]?key\s*=\s*['\"][^'\"]*['\"]" lib/ --include="*.dart"; then
          echo "⚠️ Found potential hardcoded API keys"
        fi
        
        if grep -r -i "secret\s*=\s*['\"][^'\"]*['\"]" lib/ --include="*.dart"; then
          echo "⚠️ Found potential hardcoded secrets"
        fi
        
        echo "✅ Secret scan completed"

  dependency-check:
    name: Dependency Security Check
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        cache: true
        
    - name: Get Flutter dependencies
      working-directory: .
      run: flutter pub get
      
    - name: Check for known vulnerabilities in dependencies
      working-directory: .
      run: |
        echo "🔍 Checking Flutter dependencies for known vulnerabilities..."
        
        # Check for outdated packages
        flutter pub outdated --json > outdated.json || true
        
        # Display outdated packages
        if [ -f "outdated.json" ]; then
          echo "📊 Outdated packages report:"
          cat outdated.json | jq -r '.packages[] | select(.upgradable) | "\(.package): \(.current) -> \(.upgradable)"' || true
        fi
        
        # Check pubspec.lock for known vulnerable packages
        echo "🔍 Scanning pubspec.lock for known vulnerable packages..."
        
        # List of known vulnerable packages (update as needed)
        VULNERABLE_PACKAGES="http_parser:3.1.3 path:1.6.4"
        
        for pkg in $VULNERABLE_PACKAGES; do
          if grep -q "$pkg" pubspec.lock; then
            echo "⚠️ Found potentially vulnerable package: $pkg"
          fi
        done
        
        echo "✅ Dependency security check completed"

  android-security:
    name: Android Security Check
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Check Android security configurations
      working-directory: .
      run: |
        echo "🔍 Checking Android security configurations..."
        
        # Check for network security config
        if [ -f "android/app/src/main/res/xml/network_security_config.xml" ]; then
          echo "✅ Network security config found"
          
          # Check for cleartext traffic
          if grep -q "cleartextTrafficPermitted=\"true\"" android/app/src/main/res/xml/network_security_config.xml; then
            echo "⚠️ Cleartext traffic is permitted - review for production"
          fi
        else
          echo "⚠️ No network security config found"
        fi
        
        # Check AndroidManifest.xml for security issues
        if [ -f "android/app/src/main/AndroidManifest.xml" ]; then
          echo "✅ AndroidManifest.xml found"
          
          # Check for exported activities without intent filters
          if grep -q "android:exported=\"true\"" android/app/src/main/AndroidManifest.xml; then
            echo "ℹ️ Exported activities found - ensure they are properly secured"
          fi
          
          # Check for debug flags
          if grep -q "android:debuggable=\"true\"" android/app/src/main/AndroidManifest.xml; then
            echo "⚠️ Debug flag found - should be removed for production"
          fi
          
          # Check for backup allowance
          if grep -q "android:allowBackup=\"true\"" android/app/src/main/AndroidManifest.xml; then
            echo "ℹ️ Backup is allowed - consider security implications"
          fi
        fi
        
        # Check build.gradle for security settings
        if [ -f "android/app/build.gradle" ]; then
          echo "✅ build.gradle found"
          
          # Check for minSdkVersion
          MIN_SDK=$(grep "minSdkVersion" android/app/build.gradle | grep -o '[0-9]\+' | head -1)
          if [ "$MIN_SDK" -lt 21 ]; then
            echo "⚠️ minSdkVersion is below 21 - consider security implications"
          else
            echo "✅ minSdkVersion is $MIN_SDK (acceptable)"
          fi
        fi
        
        echo "✅ Android security check completed"

  code-quality:
    name: Code Quality Check
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        cache: true
        
    - name: Get Flutter dependencies
      run: flutter pub get
      
    - name: Run Flutter analyzer for security issues
      run: |
        echo "🔍 Running Flutter analyzer for security-related issues..."
        flutter analyze --no-fatal-infos | grep -E "(avoid_print|use_build_context_synchronously)" || echo "No major security-related analyzer issues found"
        
    - name: Check for debug flags in production code
      run: |
        echo "🔍 Checking for debug flags..."
        if grep -r "debugShowCheckedModeBanner.*true" lib/; then
          echo "⚠️ Debug banner enabled in code"
        fi
        
        if grep -r "kDebugMode.*true" lib/; then
          echo "ℹ️ Debug mode checks found (this is normal)"
        fi
        
        echo "✅ Debug flag check completed"